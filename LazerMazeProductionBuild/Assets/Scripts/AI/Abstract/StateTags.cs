namespace Assets.Scripts.AI.Abstract {

    /**
     * Used as keys in a (stateTag, state) dictionary enabling fast state lookups in Enemy class
     */

    public enum StateTag { Patroling, Chasing, Attacking, Fleeing}
}
